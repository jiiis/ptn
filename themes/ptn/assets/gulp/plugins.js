'use strict';

module.exports = require('gulp-load-plugins')({
    pattern: [
        'gulp-*',
        'gulp.*',
        'del',
        'run-sequence',
        'less-plugin-autoprefix',
        'less-plugin-clean-css'
    ]
});
